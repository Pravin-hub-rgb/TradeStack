{
  "name": "tradestack",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "node dev.js",
    "start": "node dev.js --prod",
    "build": "cd frontend && bun run build",
    "build:next": "cd frontend && bun run build"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
